# Optional Target email/password for automated login helpers.
# Copy to config/target.credentials.yaml (gitignored) or use env:
#   TARGET_EMAIL / TARGET_PASSWORD
#
# Prefer `python -m pokebot login target` (real Chrome cookie export).

email: you@example.com
password: "your-password"
